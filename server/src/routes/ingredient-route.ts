import { Router } from "express";

const router = Router();

/**
 * Routes for ingredient
 * get    /ingredient
 * get    /ingredient/:id
 * post   /ingredient
 * put    /ingredient/:id
 * delete /ingredient/:id
 */

export default router;