import { Router } from "express";

const router = Router();

/**
 * Routes for food
 * get    /food
 * get    /food/:id
 * post   /food
 * put    /food/:id
 * delete /food/:id
 */

export default router;