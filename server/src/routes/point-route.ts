import { Router } from "express";

const router = Router();
/**
 * Routes for point
 * get    /point
 * get    /point/:id
 * post   /point
 * put    /point/:id
 * delete /point/:id
 */

export default router;